import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFFB94E35); // Terracotta red
  static const Color secondary = Color(0xFF2A8C7E); // Teal green for icons

  // Background colors
  static const Color background = Colors.white;
  static const Color surfaceColor = Colors.white;
  static const Color cardBackground = Color(0xFFFAF9F8);

  // Text colors
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF666666);

  // Status colors
  static const Color success = Color(0xFF2A8C7E);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFA000);
  static const Color info = Color(0xFF1976D2);

  // Icon colors
  static const Color iconPrimary = Color(0xFF2A8C7E);
  static const Color iconSecondary = Color(0xFFB94E35);
}
